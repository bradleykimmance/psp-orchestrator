// Worker bindings, generated from wrangler.jsonc + .dev.vars secret names by
// `wrangler types` (worker-configuration.d.ts). Rerun `npm run types` after
// changing bindings or adding secrets.
export type Environment = Cloudflare.Env;
